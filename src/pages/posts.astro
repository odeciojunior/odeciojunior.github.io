---
import { DEFAULT_LANGUAGE } from "@/i18n/config";

// Redirect to localized posts page
return Astro.redirect(`/${DEFAULT_LANGUAGE === 'en-US' ? 'en' : DEFAULT_LANGUAGE === 'pt-BR' ? 'pt' : DEFAULT_LANGUAGE}/posts/`, 301);
---