---
import { DEFAULT_LANGUAGE } from "@/i18n/config";

// Redirect to default language
return Astro.redirect(`/${DEFAULT_LANGUAGE === 'en-US' ? 'en' : DEFAULT_LANGUAGE === 'pt-BR' ? 'pt' : DEFAULT_LANGUAGE}/`, 301);
---
